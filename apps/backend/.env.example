# Banco de dados
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=senha
DB_NAME=roteironutri

# Servidor
PORT=3001

# Autenticação
JWT_SECRET=troque-este-valor-por-um-segredo-forte
JWT_EXPIRES_IN=7d

# Origem permitida no CORS (URL do front)
CORS_ORIGIN=http://localhost:5173
